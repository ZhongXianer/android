#include <stdio.h>
#include <stdlib.h>
/*
题目：译密码，输入一串字符，要求将其中的大小写字母变为字母表其后的四个输出，即a变为e，b变为f等，
但是最后的四个字母w、x、y和z则变为a、b、c和d，大写字母作类似的处理。
*/
int main()
{
    if(getchar() != '\n')
    {
        putchar(getchar());
    }
    return 0;
}
